apiVersion: v2
name: hasir
description: A Helm chart for deploying the Hasir stack on Kubernetes
type: application
version: 0.1.1
appVersion: "0.1.1"

dependencies:
  - name: postgresql
    version: 15.5.3
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
